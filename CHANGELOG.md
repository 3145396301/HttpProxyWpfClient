## v1.0.4

### 新功能
- 请求内容区、响应内容区、"编辑完整请求体/响应体"弹窗三个区域支持 Ctrl+滚轮 放大/缩小字体，
  三个区域各自独立记忆字号，写入 `config.json`（`RequestContentFontSize` / `ResponseContentFontSize` / `EditBodyFontSize`），下次启动自动加载。
  缩放范围为 8–40。
- 搜索侧边栏的"关闭"按钮替换为"重新搜索"按钮：会话列表出现新数据后，点击可按当前关键词/条件重新搜索。
- 搜索命中内容位于被截断部分时，提示弹窗新增"强制展示"按钮；点击后再二次确认（大文本渲染可能造成卡顿），确认后可在编辑框中加载完整内容并定位到命中位置。
- 会话列表新增"响应类型""响应长度"两列（响应完成后自动填充）；右键表头可勾选/取消展示对应列；
  展示的列与各列列宽保存到 `config.json`（`SessionColumns`），启动自动加载，关闭应用时也会保存。

### Bug 修复
- 修复搜索跳转的定位/高亮异常：原来跳转时靠 `IndexOf` 重新定位命中文本，短关键词（如 `id`）会被定位到更早出现的重复内容；且强制展示加载完整内容后立即滚动，对无换行的大文本或异步布局场景定位不准。
  现在搜索时记录命中的精确偏移量，跳转时按"头部长度 + 偏移量"换算到编辑框坐标；滚动改用 `BringCaretToView`（兼顾横向），并延迟到渲染完成后再定位。

## v1.0.3

### Bug 修复
- 修复响应体/请求体过大被截断时，"编辑完整请求体/响应体"按钮不显示的问题。
  `IsTruncated` 原来只在界面读取显示内容时被动计算，且从不通知界面刷新，
  导致按钮的显示状态永远停留在打开会话时的初始值，与内容是否真的被截断无关。
  现在改为内容变化时主动重新计算并通知界面，按钮能正确随内容状态显示、隐藏。
